# 📘 TIM Cost Optimized - User Guide

ยินดีต้อนรับสู่คู่มือการใช้งานโปรเจกต์ **TIM Cost Optimized** คู่มือนี้ถูกออกแบบมาสำหรับผู้ใช้งานทั่วไป เพื่อให้สามารถดึงโค้ดไปรัน ตั้งค่าการทำงาน และนำผลลัพธ์ที่ได้ไปใช้งานได้อย่างถูกต้องและเข้าใจง่าย

---

## 🚀 1. การเริ่มต้นใช้งาน (How to pull & install)

เพื่อให้สามารถรันโมเดลได้บนเครื่องของคุณ ให้ทำตามขั้นตอนต่อไปนี้:

**1. ดึงโปรเจกต์มาที่เครื่องของคุณ:**
เปิด Terminal (หรือ Command Prompt บน Windows) แล้วรันคำสั่ง:
```bash
git clone https://github.com/chmchnk/TIM-COST-OPTIMIZED.git
cd TIM-COST-OPTIMIZED
```

**2. สร้างและเปิดใช้งาน Virtual Environment:**
การใช้ Virtual Environment จะป้องกันไม่ให้แพ็กเกจที่ติดตั้งไปตีกับโปรเจกต์อื่นในเครื่อง ให้รัน:
```bash
python -m venv venv
```
- สำหรับ **Mac/Linux:** `source venv/bin/activate`
- สำหรับ **Windows:** `venv\Scripts\activate`

**3. ติดตั้งไลบรารีที่จำเป็น:**
```bash
pip install -r requirements.txt
```

---

## 📂 2. โครงสร้างไฟล์ที่ควรทราบ (File Structure Overview)

ในฐานะผู้ใช้งาน ไฟล์จำพวกโค้ดลึกๆ จะอยู่ในโฟลเดอร์ `script/` แต่ไฟล์ที่คุณต้องใช้งานและแก้ไขเป็นประจำมีเพียงไม่กี่ไฟล์ ดังนี้:

- ⚙️ **`config/simulation_config.yaml` (ตั้งค่า)**
  ไฟล์นี้ใช้สำหรับแก้ไขพารามิเตอร์ต่างๆ เช่น สเปคของรหัส LED (`drive_current_a`, `max_case_temp_c`), อุณหภูมิแวดล้อม (`ambient_temp_c`), สเปคของฮีตซิงก์, และความคลาดเคลื่อนต่างๆ โดยสามารถเข้าไปแก้ตัวเลขในไฟล์นี้ก่อนเริ่มรันได้เลย

- 🏃 **`script/run_pipeline.py` (ตัวสั่งรันหลัก)**
  สคริปต์นี้จะเป็นตัวรวบรวมการรันทุกสคริปต์ย่อยเข้าด้วยกัน แค่รันไฟล์นี้ไฟล์เดียว โปรแกรมก็จะทำงานออกมารวดเดียวจนได้ผลลัพธ์ (ดูวิธีการในหัวข้อ 4)

- 📊 **`data/processed/` (ตำแหน่งของผลลัพธ์)**
  เมื่อรันเสร็จสิ้น ผลลัพธ์ทั้งหมดจะถูกบันทึกไว้ที่นี่ โดยไฟล์ที่สำคัญที่สุดคือ **`recommendations.db`** (ฐานข้อมูลสำหรับนำไปใช้ต่อใน Power BI Dashboard) และรายงานเป็นไฟล์ CSV เดี่ยวๆ ในโฟลเดอร์ `data/processed/simulation/`

---

## ⚙️ 3. ภาพรวมการทำงานของระบบ (How the Pipeline Works)

ทันทีที่คุณสั่งรัน `run_pipeline.py` ตัวระบบจะทำงานตามลำดับนี้โดยอัตโนมัติ:

1. **ทำความสะอาดข้อมูล (Data Prep):** อ่านข้อมูลดิบของ TIM จากไฟล์, ลบค่าว่างที่ใช้งานไม่ได้, ทอนหน่วยให้เป็นสากล, และคำนวณตั้งราคาต่อการใช้ 1 ชิ้น
2. **จำลองความน่าจะเป็น (Monte Carlo Simulation):** ประเมินความเสี่ยงและอุณหภูมิที่อาจเกิดขึ้นจากความคลาดเคลื่อนของการทาซิลิโคน (BLT) และคุณสมบัติการนำความร้อนจากการผลิต (K) นับพันครั้ง
3. **จัดกลุ่มความคุ้มค่า (ML Clustering):** นำผลลัพธ์มาจัดกลุ่มด้วย Machine Learning เพื่อสรุปว่าวัสดุ TIM ตัวไหนคือ กลุ่มที่คุ้มค่าราคา (Best Value), ระดับใช้งานได้มาตรฐาน, กลุ่มสำหรับสเปคอุตสาหกรรม, หรือจัดอยู่ในกลุ่มที่ควรหลีกเลี่ยง (Avoid)
4. **บันทึกผลลัพธ์:** ข้อมูลจัดกลุ่มและประวัติการรันทั้งหมดจะถูกบันทึกลงฐานข้อมูล SQLite (`recommendations.db`) อย่างเป็นระเบียบ

---

## 💻 4. คู่มือการใช้คำสั่งรัน (Command Line Interface)

เวลาที่คุณพร้อมรันจำลองผลลัพธ์ ให้ใช้คำสั่งต่อไปนี้ใน Terminal (ต้องแน่ใจว่าเปิดใช้งาน Virtual environment `(venv)` แล้ว):

### 🔸 การรันแบบปกติ (รันใหม่ทั้งหมด)
```bash
python script/run_pipeline.py --scenario_name "ชื่อการจำลอง_v1"
```
ระบบจะล้างข้อมูลดิบ และคำนวณผลใหม่ทั้งหมด จากนั้นบันทึกผลลัพธ์ต่อท้ายใน ฐานข้อมูลภายใต้ชื่อ Scenario ที่ระบุไว้

### 🔸 การรันแบบข้ามการเตรียมข้อมูล (`--skip_data_prep`)
หากไฟล์ตั้งต้นของ TIM ยังเหมือนเดิม ไม่มีเพิ่มลดแถว แต่คุณ **เพิ่งเข้าไปแก้ไขค่าความร้อนใน `simulation_config.yaml` ใหม่** คุณสามารถลดเวลาการล้างข้อมูลได้ด้วยคำสั่ง:
```bash
python script/run_pipeline.py --scenario_name "ชื่อการจำลอง_v2" --skip_data_prep
```

### 🔸 การรันพร้อมลบตารางฐานข้อมูลเดิมทิ้ง (`--clear_db`)
สำหรับกรณีที่คุณอยากตั้งต้นผลลัพธ์ใหม่ทั้งหมด ไม่ให้มีข้อมูลการรันในอดีตมาปนกับข้อมูลชุดปัจจุบัน:
```bash
python script/run_pipeline.py --scenario_name "เริ่มต้นโปรเจกต์ใหม่" --skip_data_prep --clear_db
```
> [!WARNING]
> การใช้ `--clear_db` จะเคลียร์ลบข้อมูลในไฟล์ `recommendations.db` อันเก่าทั้งหมด รวมถึงข้อมูลประวัติการรันเก่าที่เคยดึงเข้า Power BI ไปแล้วด้วย ข้อมูลใหม่นี้จะเป็นแถวเดียวในฐานข้อมูล

---

## 🗂️ 5. ความสำคัญของ Metadata ใน Database (.db)

ระบบนี้ถูกออกแบบให้ใช้ฐานข้อมูล **SQLite (`recommendations.db`)** เนื่องจากไฟล์เดียวเก็บข้อมูลได้เป็นระเบียบ และสามารถบรรจุผลลัพธ์การรันได้หลายๆ รอบหรือหลายสมมติฐานให้เป็นก้อนเดียวกัน

ในไฟล์ `.db` จะมีตารางหนึ่งที่สำคัญมากคือ **`run_metadata`** ซึ่งมีหน้าที่เก็บข้อมูลการรันของคุณ ได้แก่:
- เวลาที่คุณรันโมเดลสำเร็จ (`run_timestamp`)
- **ชื่อการรัน (`scenario_name`)**: *ตัวเลือกแบ่งแยกข้อมูลชุดต่างๆ ในรอบนั้นๆ*
- ข้อมูลโมเดล ML เช่น ค่าน้ำหนักในการจัดกลุ่ม หรือคะแนน Silhouette Score

> [!IMPORTANT]
> **เหตุผลที่ต้องตั้งชื่อ `--scenario_name` ทุกครั้งโดยไม่ให้ซ้ำกัน:**
> เมื่อเชื่อมต่อฐานข้อมูลเข้าไปยัง Power BI Dashboard คุณจะต้องนำฟิลด์ `scenario_name` ไปใช้เป็น Slicer หรือ Filter ในการเลือกดูผลลัพธ์ หากคุณรันโดยที่ตั้งชื่อว่า "ทดสอบ1" ซ้ำกันสองครั้ง ข้อมูลทั้งสองรอบจะถูกรวมเข้าในพอร์ตกราฟเดียวกัน ทำให้ตัวเลขรวมใน Dashboard ขัดแย้งหรือแสดงผลซ้อนทับกันทันที 

---

## 📊 6. การนำเข้าและอัปเดตข้อมูลบน Power BI (Power BI Integration & Refresh)

เพื่อให้ Power BI Dashboard ของคุณทำงานร่วมกับฐานข้อมูลได้อย่างสมบูรณ์ ควรรู้หลักการกด Refresh ข้อมูลต่อไปนี้:

1. **กรณีรันเพิ่มเติมเพื่อนำไปทำกราฟเปรียบเทียบการตั้งค่า** 
   - สั่งรัน Pipeline ใน Terminal โดยตั้งชื่อ `--scenario_name` ให้ใหม่และ**ไม่ซ้ำเดิม**
   - คลิกปุ่ม **Refresh** ใน Power BI (หน้า Home)
   - ข้อมูลใหม่จะถูกโหลดเพิ่มเข้าไปต่อท้ายใน PBI ทำให้คุณสามารถใช้ Slicer กรองว่า จะดูกราฟของการจำลองแบบ "ชื่อการจำลอง_v1" หรือ "ชื่อการจำลอง_v2" เป็นต้น

2. **กรณีข้อมูลใน PBI เริ่มรก และต้องการตั้งต้นผลลัพธ์ใหม่ทั้งหมด**
   - รันใน Terminal โดยติด Flag การเคลียร์ฐานข้อมูล นั่นก็คือ `--clear_db` (ดังตัวอย่างด้านบน)
   - จากนั้นไปกดปุ่ม **Refresh** ใน Power BI อีกครั้ง
   - Power BI จะทำการล้างตารางเดิมออกแบบอัตโนมัติ และแสดงผลเพียง Scenario ของการรันล่าสุดที่คุณเพิ่งลงไปเท่านั้น
